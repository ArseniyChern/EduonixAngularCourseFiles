import { EduonixIfDirective } from './eduonix-if.directive';

describe('EduonixIfDirective', () => {
  it('should create an instance', () => {
    const directive = new EduonixIfDirective();
    expect(directive).toBeTruthy();
  });
});
